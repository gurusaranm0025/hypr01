1. Setup Theme option, integrate rofi and theme changes
2. Logging

FEATURES :

1.  // rootCMD.Flags().StringVarP(&powerProfileMode, "power-profile", "p", "", "change power profile (power saving, balanced, performance)")

<!-- 2.  // rootCMD.Flags().BoolVarP(&shell_hist, "shell-history", "", false, "shows zsh shell history, in rofi") -->

2.  // rootCMD.Flags().BoolVarP(&batStat, "battery-stat", "s", false, "tells the battery charge level and its status")

3.  // rootCMD.Flags().BoolVarP(&screenRes, "display-resolution", "D", false, "prints the display's resolution")

===> CRITICAL

1. Hyprlock wallpaper setting func in wallpaper module
        i. also take a look at wallpaper script.
2. finish theme setup function.
        1. monitor.conf in hyprland conf needs the monitor value of that particular monitor.
        2. Need to remove junks from logout function. (because of the moved file generating)
3. Wallpaper_selector.sh/shell_history.sh issue - fix :: finish scripts folder work
4. Fix: Floating window size (resize)
5. Shell files placing

+++ NOTES +++
1. still some paths are left as it is, in the hyprland configs (default theme)